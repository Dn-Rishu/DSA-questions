import java.util.*;
class Main {
    public static void main(String[] args) {
    Scanner sc=new Scanner(System.in);
    String str=new String("refrigerator");
    int len=str.length();
    System.out.println("the length of the refrierator:"+len);
}
}
